// Re-export the shared files screen so it's reachable from within the auth stack.
// This allows users to switch budgets or create a new one without logging out.
export { default } from '../(public)/files';
